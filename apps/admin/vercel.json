{
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --dir ../.. run db:generate && if [ \"$VERCEL_ENV\" = \"preview\" ]; then pnpm --dir ../.. run db:push && pnpm --dir ../.. run db:seed:preview; fi && pnpm --dir ../.. exec turbo build --filter=@cashback/admin",
  "crons": [
    {
      "path": "/api/cron/token-pool",
      "schedule": "0 0 * * *"
    },
    {
      "path": "/api/cron/token-release",
      "schedule": "5 0 * * *"
    },
    {
      "path": "/api/cron/marketplace-generate",
      "schedule": "10 0 * * *"
    }
  ]
}
